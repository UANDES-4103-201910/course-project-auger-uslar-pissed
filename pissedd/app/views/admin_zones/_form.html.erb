<%= form_with(model: admin_zone, local: true) do |form| %>
  <% if admin_zone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_zone.errors.count, "error") %> prohibited this admin_zone from being saved:</h2>

      <ul>
      <% admin_zone.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :zone_id %>
    <%= form.text_field :zone_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
